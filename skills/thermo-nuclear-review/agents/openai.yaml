interface:
  display_name: "Thermo-Nuclear Review"
  short_description: "Deep correctness and security branch audit"
  default_prompt: "Use $thermo-nuclear-review to audit this branch for correctness, security, and breaking changes."
policy:
  allow_implicit_invocation: false
